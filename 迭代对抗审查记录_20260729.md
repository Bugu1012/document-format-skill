# 公文格式修订.skill 迭代对抗式审查记录

审查日期：2026年7月29日
审查对象：`E:\做项目\公文格式修订.skill\` 全套 23 个技能文件（81.1 KB）
测试文档：321.docx（华文彩云 24pt）、231.docx（华文琥珀 18pt）、1234.docx（方正姚体 5pt）

---

## 第一轮：正向梳理

已有优势：

1. 定位精准。"诊断根因 + 安全修订 + 证据追溯"在现有开源技能中无对等实现。
2. 修订安全闭环。SHA-256 哈希 + 段落数验证 + 重新诊断，三份文档全部通过。
3. 接排分离。二级标题和三级标题的标题部分与正文部分按 run 级分离，实测正确。
4. 三级标题 vs 编号列表。用原始 bold 状态区分，231（标题）和 1234（列表）均正确。
5. 资料分层。SKILL.md 9,351 字节（目标 15,000 内），详细规则在 references，模板在 assets。
6. 全部 19 个文件引用路径验证通过。

## 第二轮：反向攻击

| 攻击面 | 问题 | 处理 |
|---|---|---|
| A：备份文件 | 修订脚本生成 .bak 文件，但原文件未被修改，备份多余 | 移除备份逻辑，改为"读取原文件→另存新文件" |
| B：数字字体 | ascii/hAnsi 设为 Times New Roman，数字和拉丁字母字体与中文不一致 | set_font_all 统一三个属性为同一字体 |
| C：页码偏移 | 页码在浮动文本框中，231 偏左、1234 偏右 | 清除浮动对象，重建居中页脚 |
| D：h3 误判 | 1234 的编号列表项"1.市委社会工作部"被误判为三级标题 | 用原始 bold 状态区分 |
| E：代码模板陈旧 | set_cjk_font 旧名残留、shutil 残留、缺少页脚重建和 h3 区分代码 | 全部更新 |
| F：错误图谱不全 | 缺少装饰性字体（S07）、接排未分字体（M08）、浮动文本框页码（L06） | 已补充 |

## 第三轮：证据复核

| 核验项 | 结果 |
|---|---|
| 行距 28.95pt | 三份文档修订后验证通过 |
| 页边距 37/35/28/26mm | 三份文档修订后验证通过 |
| eastAsia 属性 | 三份文档抽样验证：方正小标宋简体/黑体/楷体_GB2312/仿宋_GB2312 均正确 |
| ascii/hAnsi 属性 | 231 和 1234 验证：数字和拉丁字母使用仿宋_GB2312，不再回退 Times New Roman |
| 页码居中 | 231 和 1234 验证：align=CENTER，无浮动对象，PAGE 域存在 |
| 内容哈希 | 三份文档修订前后 SHA-256 一致 |
| 段落数 | 三份文档修订前后一致（31/45/39） |
| 文件引用 | SKILL.md 中 19 个引用路径全部存在 |
| 旧函数名残留 | 全部文件扫描：set_cjk_font=0, set_run_font=0, shutil=0, backup=0 |
| 28.8pt 残留 | 全部文件扫描：仅审查记录中描述原始问题处出现，非当前规则 |

## 第四轮：收敛判断

P0（必须修复）：全部在第二至三轮修复。
P1（应当修复）：全部修复。
P2（后续迭代）：

| 编号 | 问题 | 状态 |
|---|---|---|
| P2-1 | 评分量表权重校准 | 待更多测试文档 |
| P2-2 | 批量处理并发和错误处理 | 扩展能力，后续迭代 |
| P2-3 | 版头红线（段落边框）修订 | python-docx 局限，需 XML 操作，后续迭代 |
| P2-4 | 双面打印奇偶页页码 | 需 different_odd_even 设置，后续按需 |

收敛判断：四轮审查发现的全部 P0/P1 问题已修复并经三份真实文档验证。一致性检查零问题。技能收敛。

残余风险：
1. 方正小标宋简体字体名在不同系统上可能注册为不同名称（如"方正小标宋_GBK"），代码模板中使用的是最常见名称。
2. 浮动文本框清除操作会移除页脚中的所有内容（含非页码内容），如果页脚有其他元素需注意。
3. 结构识别对非标准文档（PDF 转换、严重混乱）的准确率未经验证。
